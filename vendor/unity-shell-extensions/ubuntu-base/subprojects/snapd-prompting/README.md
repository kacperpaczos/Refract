Just install the snapd-prompting@canonical.com directory in your ~/.local/share/gnome-shell/extensions and enable it!

To get the test app running you need to use some classic snap ids as per `snap list | grep classic`, so adapt them if you don't have the default values installed
